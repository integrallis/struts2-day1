package com.integrallis;

import com.opensymphony.xwork2.ActionProxy;
import com.opensymphony.xwork2.ActionSupport;
import org.apache.struts2.StrutsTestCase;
import static org.junit.Assert.assertTrue;
import static org.junit.Assert.assertEquals;

public class TodoActionTest extends StrutsTestCase {

/*
    public void testSaveAction() throws Exception {
System.out.println("Here we go!!");
      // Set requests
      request.setParameter("name", "Joseph");
      request.setParameter("save_name", "Joseph2");      
      request.setParameter("description", "Joseph3");
            
      // then run the proxy for it.
    	ActionProxy proxy = getActionProxy("/todo/save");
      TodoAction action = (TodoAction) proxy.getAction();
      
      String result = action.execute();
System.out.println("The result " + result);
        assertEquals(result, "/todo/finish.jsp");
    }
    
    public void xtestSaveActionWithErrorrs() throws Exception {      
      // then run the proxy for it.
    	ActionProxy proxy = getActionProxy("/todo/save");
      TodoAction action = (TodoAction) proxy.getAction();
      
      String result = action.execute();
System.out.println("The result " + result);
        assertEquals(result, "/todo/finish.jsp");
    }
    */
}